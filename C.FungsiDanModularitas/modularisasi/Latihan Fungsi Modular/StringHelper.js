
export function keHurufBesar(teks) {
    teks = teks.toUpperCase();

    console.log(teks);

    return
}

export function potongTeks(teks, panjang) {
    teks = teks.slice(0, panjang);

    console.log(teks);

    return

}

export const versi_modul = "1.0.0";